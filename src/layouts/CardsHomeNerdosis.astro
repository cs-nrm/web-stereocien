---
/*import DoubleBox from '../components/DoubleBox.astro';
import BoxBanner from '../components/BoxBanner.astro';
import LeaderBoard from'../components/LeaderBoard.astro';
import SuperLeader from '../components/SuperLeader.astro';
import { Image } from 'astro:assets';*/


const res = await fetch("https://stereociendigital.mx/wp-json/wp/v2/posts?_fields=title,slug,yoast_head_json,_links,_embedded&_embed&per_page=2&categories=531");
const posts = await res.json();

---
<!--div class="flex flex-wrap">
{
posts.map((post:any)=> (
    <a class="text-black hover:text-[#7b439b] w-full sm:w-1/2 lg:w-full px-8 mb-8" href={`/entrevistas/${post.slug}/`}>
        <Image class="block h-auto w-full aspect-video shadow-[0_2px_12px_7px_rgba(0,0,0,0.4)] rounded object-fill" src={post.yoast_head_json.og_image[0].url} width="300" height="150" alt={post.title.rendered}  />
        <p class="font-bold leading-none my-4 text-lg text-center" set:html={post.title.rendered}></p>
    </a>
))    
}
</div-->
{
posts.map((post:any)=> (
<div class="card w-full md:w-[48%] md:mr-[2%] mb-8">
    <article>
    <div class="glows"></div>
    <div class="overflow-hidden rounded-lg">
        <a href={`/nerdosis/${post.slug}/`} >
            <div class="flex items-center justify-between leading-tight">
              <img class="block w-2/4 aspect-video" src={post.yoast_head_json.og_image[0].url} alt={post.title.rendered} />
              <h2 class="text-nerd text-base text-zinc-300 font-bold p-2" set:html={post.title.rendered}></h2>
            </div>
        </a>
    </div>
    </article>
</div>
))
}  

<div class="m-auto text-center">
    <a href="/nerdosis">
        <button class="ver-mas group relative h-8 w-36 m-4 overflow-hidden rounded-lg bg-zinc-700 text-base shadow">
            <div class="absolute inset-0 w-3 bg-[#09e6f1] transition-all duration-[250ms] ease-out group-hover:w-full"></div>
            <span class="relative text-zinc-200 group-hover:text-zinc-900">Ver más</span>
        </button>
    </a>
</div>

